
colcon build --symlink-install
# 或只编译指定包：
colcon build --packages-select tomogram_rsc tomography pct_planner --symlink-install


   source install/setup.bash
   ros2 launch tomography tomography.launch.py       #scene_name:=building
   
   
      source install/setup.bash
   ros2 launch pct_planner planner.launch.py         #scene_name:=building
   
   
# 终端 1
   source install/setup.bash &&
ros2 launch tomography tomography.launch.py scene_name:=plaza

# 终端 2（等待 tomography 完成）
      source install/setup.bash &&
ros2 launch pct_planner planner.launch.py scene_name:=Plaza
   
   
   
   
   
   
   
