
cd /home/jie/pctplanner444_yan_3D_ws && colcon build --packages-select pct_planner


# PCT Planner 完整启动流程

## 前置准备

确保工程已编译：
```bash
cd ~/pctplanner444_yan_3D_ws
colcon build --symlink-install
```

---

## 完整启动流程

### 终端1：启动 Tomography 节点（生成 tomogram）

```bash
cd ~/pctplanner444_yan_3D_ws
source install/setup.bash
ros2 launch tomography tomography.launch.py scene_name:=Building

ros2 launch tomography tomography.launch.py scene_name:=plaza
```

**说明：**
- `scene_name:=plaza` 使用小写（tomography 节点要求小写）
- 会启动 tomography 节点和 RViz2
- 等待看到日志：`Tomogram exported: plaza3_10.pickle`

**可用场景：**
- `scene_name:=plaza` → 广场场景
- `scene_name:=building` → 建筑场景
- `scene_name:=spiral` → 螺旋场景

---

### 终端2：启动 Planner 节点（路径规划）

```bash
cd ~/pctplanner444_yan_3D_ws
source install/setup.bash
ros2 launch pct_planner planner.launch.py scene_name:=Building

ros2 launch pct_planner planner.launch.py scene_name:=Plaza
```

**说明：**
- `scene_name:=Plaza` 使用首字母大写（planner 节点要求首字母大写）
- 节点会先用默认起点/终点规划一次
- 之后可以在 RViz 中通过 `Publish Point` 按钮选择新的起点和终点重新规划

**可用场景：**
- `scene_name:=Plaza` → 对应 `plaza3_10.pickle`
- `scene_name:=Building` → 对应 `building2_9.pickle`
- `scene_name:=Spiral` → 对应 `spiral0.3_2.pickle`

**注意：** tomography 和 planner 的场景名称要匹配！

---

## 在 RViz 中通过 Publish Point 交互设置 3D 起点和终点

1. **程序启动后的终端提示**：
   - Planner 节点启动后，会在终端打印：`请输入导航起点`

2. **设置起点（第 1 次点击 Publish Point）**：
   - 在 RViz 顶部工具栏选择 `PublishPoint` 工具（配置文件中已将话题设为 `/clicked_point`）
   - 在点云/地图中点击你想要的起点位置（可以直接点在点云上，支持 3D）
   - 终端会打印：`导航起点坐标为：(x, y, z)`，然后提示：`请输入目的地位置：`

3. **设置终点并规划（第 2 次点击 Publish Point）**：
   - 继续使用同一个 `PublishPoint` 工具，在点云/地图中点击目的地位置
   - 终端会打印：`目的地坐标为：(x, y, z)`，然后提示：`下面进行路径规划：`
   - PCT Planner 使用这两个 3D 点进行路径规划，规划完成后终端打印：`规划已完成，请查看`，随后再次打印：`请输入导航起点`，可以继续下一次规划

4. **高度（z 值）的处理**：
   - 如果点击的点自带非零 z 值，则直接作为高度使用
   - 如果 z 为 0 或接近 0，程序会从 tomogram 中自动查询该 (x, y) 位置的高度并填充

5. **查看路径**：
   - 在 RViz 左侧 `Displays` 中添加 `Path` 显示
   - 订阅话题：`/pct_path`
   - 每次完成规划后，`/pct_path` 中的 3D 轨迹会自动更新

---

## 默认起点和终点坐标

### Plaza 场景
- **起点**：`[0.0, 0.0]`
- **终点**：`[23.0, 10.0]`

### Building 场景
- **起点**：`[5.0, 5.0]`
- **终点**：`[-6.0, -1.0]`

### Spiral 场景
- **起点**：`[-16.0, -6.0]`
- **终点**：`[-26.0, -5.0]`

---

## 常见问题

### 1. 找不到 tomogram pickle 文件
**解决**：确保先启动 tomography 节点，并等待看到 `Tomogram exported: ...` 日志

### 2. 场景名不匹配
**解决**：
- tomography 用 `scene_name:=plaza`（小写）
- planner 用 `scene_name:=Plaza`（首字母大写）

### 3. RViz 中看不到路径
**解决**：
- 在 RViz 左侧 `Displays` 中添加 `Path` 显示
- 订阅话题：`/pct_path`
- 确保 Fixed Frame 设置为 `map`

### 4. 路径规划失败（Failed to generate trajectory）
**原因**：
- 起点和终点之间被障碍物隔开
- 起点或终点不在 tomogram 范围内
- 换一个更合理的起点/终点位置

---

## 停止程序

在对应终端按 `Ctrl+C` 即可停止。
